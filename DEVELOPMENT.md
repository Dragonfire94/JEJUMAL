# 제주말, 이렇게 만들어 왔습니다

이 문서는 제품 대화에서 정한 것과, 코드에 실제로 넣은 것을 같이 적습니다.  
저장소: [Dragonfire94/JEJUMAL](https://github.com/Dragonfire94/JEJUMAL)

앱은 **소리로 배우는 제주어**입니다. 웹 먼저, 기록은 이 기기 `localStorage`, 발음은 제주특별자치도 제주어 사전을 로컬 mp3로 둡니다. 로그인·서버 DB는 켜지 않았습니다.

---

## 1. 콘텐츠

도청 Open API 원본은 약 7,159개입니다. 처음부터 다 넣지 않고, 배울 만한 말만 골랐습니다.

| 단계 | 규모 | 이유 |
|---|---|---|
| 첫 앱 | 400단어 | 퀴즈·복습 루프를 먼저 검증 |
| 지금 | 1,000단어 · 100유닛 · 유닛당 10개 | 해녀 등급 5단계 × 200단어 |

**빼기로 한 것:** 배울 필요 없는 표제어, 품사·뜻이 뒤섞인 행, 같은 유닛 안에서 의미 밀도만 낮추는 중복.

**고친 사전 오류 (확실 2 + 의심 1 + 학습 효과 없는 슬롯):**

- `담다`→김치 오매핑 → 담그다
- `팔`→삼 → 눈의 삼이 아니라 팔
- `옵서`→오다 → 오세요 (명령형)
- 돌하르방·땅콩·강낭콩처럼 제주어=표준어인 슬롯은 교체 검토

같은 뜻에 방언 어형이 여러 개인 것(하르방/하르바님, 항긋/큰한집 등)은 오류가 아니라 사전 특성입니다.

발음은 도청 서버를 매 문제마다 치지 않습니다. `public/audio/{seq}.mp3`로 받아 두고, 파일이 없으면 TTS로만 넘어갑니다.

---

## 2. 해녀 등급과 해금

처음에 칸이 잠금처럼 보였지만 실제로는 다 열려 있었습니다. 검수 때문에 잠깐 푼 적도 있습니다. 지금은 아래가 규칙입니다.

| 등급 | 메시지 | 열리는 때 |
|---|---|---|
| 애기해녀 | 혼저옵서예. 발만 적신 날 | 처음부터 |
| 하군 | 얕은 바당, 숨 맞춰 봅주 | 애기해녀 200단어 중 **120**(60%) |
| 중군 | 놀멍 배우멍, 말이 붙었수다 | 하군 120 |
| 상군 | 숨비소리 한 번, 깊게 잠수멍 | 중군 120 |
| 대상군 | 이 바당의 대상군이우다 | 앞 네 등급을 **전부** 클리어 |

중요한 합의:

- 다음 등급은 **그 등급 카드의 60%**로 열립니다. 애기해녀를 100% 채우고 하군을 몇십 퍼센트만 했다고 중군이 열리지 않습니다.
- 대상군만 예외로 마스터 게이트입니다.
- 홈 칭호는 전체 1,000단어의 20·40%가 아니라 **지금 열린 가장 높은 등급**입니다. 진행바 눈금(20/40/60/80)은 칭호와 어긋나서 뺐습니다.
- 상태창 카피는 “하군까지 200단어”가 아니라 **120단어**입니다.
- 잠긴 탭을 누르면 해금 조건이 그 화면에서 보입니다. 대상군은 문장만 두고 등급별 해금 현황 목록은 없습니다.

한동안 “등급을 칸마다 또 나누자 / 해금 게이지 0–100%” 이야기도 있었습니다. 지금은 **기초 100단어 묶음이 아니라, 등급당 200단어를 60%로 넘기는 칸 구조**로 고정했습니다.

---

## 3. 배우기 (퀴즈)

칸을 열면 10단어를 뜻·소리로 보여 준 뒤 듣기 10 + 읽기 10입니다. 별도 수업 화면은 만들지 않기로 했습니다. 이미 미리보기가 있습니다.

- 클리어는 **70%**. 못 넘기면 유닛은 안 끝납니다.
- **틀린 것만 다시** 풀 수 있습니다.
- 재도전까지 떨어져도 다음 칸으로 건너뛰지 않습니다. 대신 **복습노트에서 이어서**로 나갑니다.
- 맞혀도 틀려도 노트에 넣을 수 있고, 잘못 누르면 취소됩니다.
- 정답/오답/결과 효과음. 설정에서 끌 수 있습니다.
- 보기 부족하면 같은 유닛 → 같은 품사 → 전체 풀 순입니다.
- 스크린리더: 아이콘은 `aria-hidden`, 결과는 `role="status"`.

**아직 없는 것:** 용례 문장. 사전에는 있는데 앱은 `제주어 ↔ 표준어` 한 줄입니다. 수업 화면을 더 만드는 것보다 문장이 다음 콘텐츠 숙제입니다.

홈:

- 주 버튼은 **이어서 배우기**
- 오늘 볼 카드가 있을 때만 **복습 N장**
- 스트릭 숫자는 홈에 안 올립니다. 통계에 있습니다.

---

## 4. 복습노트 (SRS)

오답 자동 적립이 아니라, 본인이 넣는 노트입니다. 맞혀도 넣을 수 있습니다.

- 카드. 누르면 뒤집히며 뜻이 나옵니다.
- 알아도 바로 삭제하지 않습니다. `아직` / `알아요` / `노트에서 빼기`
- 간격은 단순 사다리 (1 → 3 → 7 → 14…). 맞히면 늘고, 틀리면 1일로 리셋.
- 정렬은 복습 예정일이 지난 것 먼저.
- 탭 배지는 노트 전체가 아니라 **오늘 볼 카드(due)** 수입니다.

---

## 5. 통계

개인 학습 기록입니다. 운영자 대시보드가 아닙니다.

있는 것: 오늘 신규·복습, 연속 일(복습만 해도 이어짐), 전체 %, 14일 학습량·정답률, **등급별** 완료율, 복습 대기 수.

뺀 것:

- 주제별 바 — 배우기 칸과 겹침
- 통계에 해금 문구, 복습노트 “보러 가기” CTA — 할 일이 섞임

일별 집계 `dailyStats`는 이벤트 로그가 아니라 하루 한 레코드입니다. 90일 지나면 정리합니다. 날짜 키는 로컬 달력일(`localDateKey`)이고, SRS의 24시간 간격과 다른 함수입니다.

---

## 6. 설정

단어마다 “이 말 이상해요”를 달지 않기로 했습니다. 학습 화면에 개발자용 버튼이 됩니다.

하단 탭: **배우기 / 복습 / 통계 / 설정**

| 넣은 것 | 안 넣은 것 |
|---|---|
| 밝게·어둡게·시스템 | 로그인·동기화 (웹 고도화 다음) |
| 효과음 | 꺼 둔 폰을 깨우는 푸시 (서버 필요) |
| 복습 알림 — **앱을 연 상태**에서 오늘 카드가 있을 때, 하루 1회 | 다크모드를 zinc로 바꾸는 것 (갈색 톤 유지) |
| 콘텐츠 제보 — 방금 본 말 자동 채움 | 이용약관 가짜 문서 |
| 앱이 이상할 때 — 설명 + 선택적으로 멈춘 기록 | 최근 오류 원문을 설정 맨 위에 노출 |
| 학습 기록 지우기 | |

크래시: `reportError`는 콘솔 + 이 기기 저장입니다. Sentry는 공개 배포 때 `track()`/`reportError` 자리만 바꾸면 됩니다. 학습자에게는  techn 메시지를 안 보여 줍니다. 멈춘 화면에 **다시 열기 / 문제 알리기**, 설정에는 **방금 멈춘 기록도 같이 보내기**만 있습니다.

제보 채널은 GitHub 이슈입니다. 비개발자에게는 장벽이 있습니다. 백엔드 없이 시작하는 절충입니다.

---

## 7. 운영자가 아직 못 보는 것

`/stats`는 한 사람의 기록입니다. DAU, 유닛별 오답, 70% 도입 전후 완주율은 없습니다.

그래서 `track()`만 심었습니다. 이벤트:

`unit_start` `unit_pass` `unit_fail` `retry_start` `notebook_add` `review_done` `rank_open`

그래서 `track()`만 심었습니다. 이벤트:

`unit_start` `unit_pass` `unit_fail` `retry_start` `notebook_add` `review_done` `rank_open`

단어 텍스트는 안 넣습니다. 지금은 `console.info`이고, `LIVE_HOSTS`가 비어 있어 외부로 안 나갑니다. 공개 주소가 생기면 호스트를 목록에 넣고 PostHog(`autocapture`·세션 리플레이 끔)와 Sentry를 그때 설치합니다.

---

## 8. 일부러 안 건드린 것

- **소셜 로그인, 네이티브 앱:** 웹 루프가 먼저입니다.
- **Sentry / Plausible 계정:** 사용자가 없는 프리뷰에 붙여도 개발자 클릭만 쌓입니다.
- **`npm run build`의 `db:migrate`:** 플랫폼 기본입니다. `DATABASE_URL` 없거나 맨 위 SQL이 없으면 연결하지 않고 성공합니다. 빼면 나중에 로그인 켤 때 더 위험합니다. auth/db는 여전히 OFF.
- **정식 이용약관·개인정보처리방침:** 앱스토어·공개 마케팅 전에 필요합니다. 지금은 가짜 약관 대신, 필요할 때 설정에 “이 기기에만 남음 / 로그인 없음 / 제보는 GitHub” 안내를 쓰면 됩니다.

테스트: `npm test`는 앱 vitest 15개만 돌립니다. 플랫폼 스크립트 테스트는 `npm run test:scripts`입니다. (제목 `제주말` 때문에 OG 픽스처 몇 개가 깨져 있어 기본 명령에서 뺐습니다.)

---

## 9. 커밋으로 보는 줄기

최근이 위입니다. 메시지가 결정문입니다.

```
18ca53a  학습자에게 크래시 원문 숨김
a04a4e9  track() 이벤트
099e713  최근 오류·방금 본 말
967ad3a  다크모드, 당일 복습 알림
9c11eb8  설정 탭, 단어별 신고 삭제
117c23b  npm test = vitest
94cc8ca  통계를 등급별로
8e0cfa6  해녀 등급 카피
b2cd69b  칭호 = 열린 가장 높은 등급
c7a372b  홈·배지에 오늘 due
d623e69  vitest, 재도전 탈출
df93b0f  120단어면 다음 등급, 대상군은 전부
f1ea573  통계 탭
6216913  도청 원본 음
0418baf  70% + 틀린 것만 다시
aca20b5  오답 보기 3단계
edbef02  퀴즈 접근성
825ca02  담그다 / 팔 / 오세요
```

---

## 10. 다음에 손대면 좋은 것

1. 용례 문장 — 사전 데이터를 칸 미리보기에 한 줄
2. 공개 배포 후 `track()` → Plausible, `reportError` → Sentry
3. 계정 동기화 — 폰을 바꾸면 지금 진도가 사라짐
4. 짧은 개인정보 안내 페이지 (스토어 전)
5. 푸시 — 네이티브 또는 서버가 생긴 뒤
